trigger:
  - main

pool:
  name: Default

steps:
#- task: UseDotNet@2
#  inputs:
#    packageType: 'sdk'
#    version: '5.x'
    
- script: |
    echo "Pipeline Test Start"
    echo "$[counter('', 4)]"
    echo "Pipeline Test End"
  displayName: "Run Script"
