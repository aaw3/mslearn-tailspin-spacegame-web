trigger:
  - main

pool:
  name: Default

steps:
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '5.x'
    
- script: |
    dotnet --version
    echo "Pipeline Test"
    displayName: "Run Script"
